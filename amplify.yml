version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run build:prod
    build:
      commands:
        - npm run build
  artifacts:
    files:
        - '**/*'
    baseDirectory: dist/
  cache:
    paths:
      - node_modules/**/*
